{
    "name": "@fv-studio/server",
    "version": "0.0.1",
    "description": "",
    "author": "",
    "private": true,
    "license": "UNLICENSED",
    "scripts": {
        "build": "nest build",
        "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
        "start": "nest start",
        "dev": "nest start --watch",
        "start:debug": "nest start --debug --watch",
        "start:prod": "node dist/main",
        "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix",
        "test": "node -e \"console.log('NestJS tests are temporarily disabled in this workspace.')\"",
        "test:watch": "node -e \"console.log('NestJS tests are temporarily disabled in this workspace.')\"",
        "test:cov": "node -e \"console.log('NestJS tests are temporarily disabled in this workspace.')\"",
        "test:debug": "node -e \"console.log('NestJS tests are temporarily disabled in this workspace.')\"",
        "test:e2e": "node -e \"console.log('NestJS tests are temporarily disabled in this workspace.')\""
    },
    "dependencies": {
        "@nestjs/common": "^11.0.1",
        "@nestjs/core": "^11.0.1",
        "@nestjs/platform-express": "^11.0.1",
        "reflect-metadata": "^0.2.2",
        "rxjs": "^7.8.1"
    },
    "devDependencies": {
        "@nestjs/cli": "^11.0.0",
        "@nestjs/schematics": "^11.0.0",
        "@types/express": "^5.0.0",
        "source-map-support": "^0.5.21",
        "ts-loader": "^9.5.2",
        "ts-node": "^10.9.2"
    }
}
